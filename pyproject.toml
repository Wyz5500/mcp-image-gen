[project]
name = "image-gen"
version = "0.1.0"
description = "MCP server that generates images via ChatGPT + Playwright"
requires-python = ">=3.12"
dependencies = [
    "mcp",
    "playwright",
]

[tool.uv]
package = false
